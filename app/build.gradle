plugins {
    id 'com.android.library'
}

android {
    namespace 'com.velorexe.unityandroidble'
    compileSdk 34

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    lint {
        baseline = file("lint-baseline.xml")
    }
}

dependencies {
    implementation files('libs/unity.jar')
    implementation 'com.github.polarofficial:polar-ble-sdk:5.10.0'
}

/*
Not needed for creating .aar, but if sb wants to use them, here they are. First part needs to be switched back to
plugins {
    id 'com.android.application'
}

tasks.register('deleteJar', Delete) {
    group('jar')
    delete 'libs/unityandroidble.jar'
}

tasks.register('createJar', Copy) {
    group('jar')
    from('build/intermediates/compile_app_classes_jar/release/')
    into('libs/')
    include('classes.jar')
    rename('classes.jar', 'unityandroidble.jar')
}

createJar.dependsOn(deleteJar, build)*/